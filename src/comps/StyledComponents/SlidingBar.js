import React from 'react';
//this is a sliding div which is a reusable component for all filters and collections
const SlidingBar = ({collections}) => {
  return <div>SlidingBar</div>;
};

export default SlidingBar;
